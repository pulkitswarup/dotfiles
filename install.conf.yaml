- defaults:
    link:
        create: true
        relink: true

- clean: ['~']

- brewfile:
    file: Brewfile

- link:
    ~/.dotfiles: ''

- shell:
    - [git submodule update --init --recursive, Installing submodules]
